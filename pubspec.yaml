name: Vanto
description: Aplicația Vanto pentru Android!
version: 5.0.7+2020041400

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  firebase_core: ^0.4.4+2
  firebase_analytics: ^5.0.11
  firebase_auth: ^0.15.5+2
  firebase_storage: ^3.1.3
  cloud_firestore: ^0.13.4+1
  url_launcher:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons:

flutter:
   uses-material-design: true

flutter_icons:
  image_path: "assets/pictogram_round.png"
  android: true # can specify file name here e.g. "ic_launcher"
  adaptive_icon_background: "assets/pictogram_background.png" # only available for Android 8.0 devices and above
  adaptive_icon_foreground: "assets/pictogram_foreground.png" # only available for Android 8.0 devices and above

